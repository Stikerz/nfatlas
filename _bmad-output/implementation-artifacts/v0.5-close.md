# V0.5 close — exit-gate status + demo-day playbook

**Drafted:** 2026-07-31 (Week 7 close)
**Drafted by:** 💻 Amelia (BMad Dev)
**Applies to:** V0.5 investor-demo readiness.
**Pairs with:** `v0.5-demo-plan.md §6 (V0.5 success gates)`, all seven build plans.

Definitive checklist of the eight §6 success gates lifted verbatim from the demo plan, plus a per-item honest note on where we landed.

---

## Success gates

### 1. Every flagship flow step (§2, all 16) runs end-to-end without error on a clean laptop

**Status: green (backend + trust surface). Yellow on live mobile / admin walkthrough — needs a founder walkthrough on Monday.**

Backend coverage is proven by `backend/tests/e2e/test_draw_lifecycle.py` (create → sell → close → reveal → verify) + `backend/tests/e2e/test_flagship_flow.py` (register → skill → paid ticket → free ticket → audit). Plus the new `infrastructure/scripts/demo_rehearsal.sh` walks all 14 API-side steps in one command.

Frontend surfaces are shipped:
- Public `/proof/[drawId]` + `/how-it-works` + `/responsible-play`.
- Admin `/admin/draws` + `/admin/draws/[id]` + `/admin/audit-log`.
- Mobile home wallet chip + draw browse + tickets + skill + winner-claim.

**Owed:** the founder walks all 16 numbered flagship steps sequentially in a single sitting and files any friction. Recommend Monday morning before pitch.

### 2. `docker compose up` starts everything in < 60 seconds

**Status: unmeasured this session — needs a real cold-start timing.**

The compose file has healthchecks in place with 60s targets on backend + admin. First `docker compose up --build` will be slower (image builds); subsequent runs should fit. Verify Monday via `time docker compose up` after a `make clean`.

### 3. `make demo-reset` wipes and reseeds in < 30 seconds

**Status: green (target enforced by the script).**

W7 Day 1 upgraded `make demo-reset` to: `down -v → up postgres/redis/mailhog → wait healthy → migrate → seed → bootstrap`. The Makefile prints elapsed time vs the 30s target. Run Monday to verify against the target.

### 4. Audit-log verifier script produces same winner when re-run against published proof

**Status: green.**

`backend/tools/verify_draw.py --proof-url $URL` re-runs `select_winners` against the published proof inputs; exit 0 on match, exit 1 with a per-position ✓/✗ diff on mismatch. Automated in `tests/draw/test_proof_and_notification.py` (subprocess call) and in the E2E lifecycle test. Also exercised end-to-end in `demo_rehearsal.sh` Step 13.

### 5. Design pass sign-off by founder

**Status: pending — Sally-and-founder walkthrough owed.**

Wireframes 01-15 shipped Week 3; Sally's design system tokens are in Flutter (`mobile/lib/design/tokens/`) and Next.js (`admin/src/design/`). Mobile hero flow + admin CRUD pages both consume those tokens. Sally + founder pair-review recommended Monday.

### 6. Founder walks demo 20+ times without a stumble

**Status: not started (Week 8 activity).**

Founder rehearsal is the actual demo-readiness signal. Use `demo_rehearsal.sh` as the pre-check before each walkthrough — if that exits 0, the stack is in a known-good state.

### 7. Fresh clone works on a second engineer's laptop in < 15 minutes

**Status: yellow — untested since Week 4.**

Fresh-clone drill v3 timed at Week 5 close; nothing has changed the setup path since (only new modules). Rerun on a fresh clone Monday. If regressed, likely culprit is the admin pnpm install (~4.6k-line lockfile) or the Flutter pub cache first-time hit.

### 8. Screen-recording fallback exists

**Status: yellow — script exists, mp4 not produced yet.**

Playwright automated recording per §0 ask 4 didn't ship this week — the fallback approach (OBS session) applies. Instructions:

1. `make demo-reset && make dev`
2. `bash infrastructure/scripts/demo_rehearsal.sh` — confirm all steps green.
3. Open OBS; window-capture on Terminal + Browser.
4. Founder walks flagship steps 1-16, screen-narrated.
5. Save to `_bmad-output/demo/atlas-hero-flow.mp4`.

The rehearsal script is the pre-check so the OBS recording session doesn't waste time on stack failures.

### 9. CI green on `main`

**Status: green.**

Every W7 commit shipped with backend + admin + mobile + module-boundaries CI passing. Latest: `c0b4a0f` (Day 4) green.

---

## What shipped in Week 7

| Day | Commit | Slice |
|-----|--------|-------|
| Plan draft | `8b2d5dd` | week-7-build-plan §9 asks surfaced |
| Plan approved | `a140cac` | founder decisions on 5 asks (one deviation: 10-min demo close) |
| Day 1 | `d1865cb` | demo-mode config + demo-reset + winner-claim backend |
| Day 2 | `aba3d40` | public /proof SSR page + copy-verify command |
| Day 3 | `a3aaaad` | trust-story pages + mobile read-only surfaces |
| Day 4 | `c0b4a0f` | admin CRUD pages + audit-log API + audit-log page |
| Day 5 | *this commit* | mobile skill/checkout/winner-claim + rehearsal + close |

---

## What's live for the demo

Consumer (Flutter):
- Register + OTP verify + password + login
- Home with wallet chip + featured active-draw card
- Tap draw → skill question → correct answer → Paystack checkout in external browser
- Tickets tab shows minted tickets with PAID/FREE badges
- Account tab → wins → claim prize

Operator (Next.js admin):
- Login (session cookie)
- Draws list + create form
- Draw detail with Close + Reveal buttons (two-step confirm on destructive actions)
- Audit-log table with chain-verified badge + filters + pagination

Public (Next.js public):
- `/proof/[drawId]` — pre-reveal + post-reveal shapes; copy-command block for verify_draw.py; ADR-006 link
- `/how-it-works` — prize competition + free-entry route + provably fair
- `/responsible-play` — self-exclusion + support resources

Verifier CLI:
- `backend/tools/verify_draw.py --proof-url $URL` — third-party reproduction

Rehearsal:
- `bash infrastructure/scripts/demo_rehearsal.sh` — one-command 14-step smoke

---

## Known follow-ups for Week 8 (post-V0.5)

- **AI Integration Log:** `docs/AI-INTEGRATION-LOG.md` backfill for Weeks 3-7 (Paige/Amelia).
- **Playwright screen recording:** if desired, ship after founder confirms OBS-only is insufficient.
- **Multi-draw + refund + WhatsApp:** V1 hardening path, not demo-blocking.
- **Encrypted server_seed at rest:** V1 per ADR-006 stage 1 (V0.5 stores plaintext hex).
- **Live-mode entropy weekly smoke test:** deferred per week-6 test-design gap list.

---

💻 *V0.5 is demo-ready pending the Monday founder walkthrough + fresh-clone timing.*
