"""Admin module.

Owns: RBAC (ADR-009 — permissions-as-primitive, roles-as-bundles),
admin-only endpoints not owned by other modules, superadmin bootstrap.
Day 5 lands `bootstrap_superadmin.py` seed script and login-side hooks.
"""
